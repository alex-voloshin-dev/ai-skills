# Launch Reference

This file is a generic placeholder for the Windsurf-side equivalent of a Claude launch configuration.

## Recommended Shape

| Component | Command | Port | Notes |
|---|---|---|---|
| `service-name` | `start-command` | `port` | `optional notes` |

## Usage

- replace the placeholder rows with project-specific services
- keep commands project-local, not package-local
- keep root `AGENTS.md` as the source of truth for actual topology
