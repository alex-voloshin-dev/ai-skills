# `AGENTS.md` Template: TypeScript React Next.js

```md
# [Project Name] Frontend Guidelines

## Stack

- TypeScript [version]
- Next.js [version]
- React [version]
- UI system: [MUI / Tailwind / custom]

## Hard Rules

- Keep server and client boundaries explicit
- Validate external inputs at the edge
- Keep metadata and rendered content in sync

## Structure

- `app/` — routes and layouts
- `src/components/` or `components/` — reusable UI
- `src/lib/` or `lib/` — hooks, API clients, utilities
- `public/` — static assets

## Commands

- Install: `[yarn install]`
- Run: `[yarn dev]`
- Build: `[yarn build]`
- Test: `[yarn test]`
- Lint: `[yarn lint]`

## UI Conventions

- [design system rule]
- [component export rule]
- [accessibility rule]

## Content and SEO

- update metadata when visible copy changes
- keep JSON-LD aligned with visible content
- note any content source files and page dependencies
```
