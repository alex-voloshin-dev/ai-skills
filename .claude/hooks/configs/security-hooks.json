{
  "hooks": {
    "pre_write_code": [
      {
        "command": "python3 .claude/hooks/scripts/block-secrets-in-code.py",
        "working_directory": "${workspaceFolder}",
        "description": "Scan edits for hardcoded secrets (API keys, tokens, passwords, private keys)"
      }
    ],
    "pre_run_command": [
      {
        "command": "python3 .claude/hooks/scripts/block-dangerous-commands.py",
        "working_directory": "${workspaceFolder}",
        "description": "Block destructive commands (rm -rf, DROP DATABASE, terraform destroy, etc.)"
      }
    ],
    "pre_read_code": [
      {
        "command": "python3 .claude/hooks/scripts/block-sensitive-files.py",
        "working_directory": "${workspaceFolder}",
        "description": "Prevent reading .env, private keys, credentials, and other sensitive files"
      }
    ]
  }
}
