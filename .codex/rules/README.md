# Codex Rules

These rules are Codex-native equivalents for reusable guidance previously stored under `.claude/rules`.

Use them when:

- updating `AGENTS.md`
- validating AI asset changes
- designing Codex workflows and safeguards

They are reference documents, not implicit runtime hooks.
